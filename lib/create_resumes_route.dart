import 'package:flutter/material.dart';

class CreateResumesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        brightness: Brightness.dark,
        title: Text('Create Resumes'),
      ),
    );
  }
}
